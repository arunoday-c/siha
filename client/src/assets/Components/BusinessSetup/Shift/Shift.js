import React, { Component } from 'react';

class Shift extends Component {
    render() {
        return (
            <div>
                SHIFT
            </div>
        );
    }
}

export default Shift;