{{{importStyle "css/algaehReportStyles.css"}}}

<body>
  <div class="print-body">

    <section>
      <table class="tableForData" cell-padding="0">
        <thead>
          <tr>
            <th>Sl No.</th>
            <th>Primary ID</th>
            <th>Patient Code</th>
            <th>Patient Name</th>
            <th>Outstanding Amt.</th>

          </tr>
        </thead>
        <tbody>
          {{#hasElement result}}
            <tr class="numberFldTotalBottom">
              <td class="txtFld" colspan="4"></td>
              <td class="txtFld">{{net_balance_credit}}</td>
              {{!-- <td class="numberFld" colspan="3">Age: {{return_age}}</td> --}}
            </tr>
            {{#each result}}
              <tr>
                <td>{{inc @index}}</td>
                <td>{{primary_id_no}}</td>
                <td>{{patient_code}}</td>
                <td style="text-align: left;">{{full_name}}</td>
                <td>{{advance_amount}}</td>
              </tr>
            {{/each}}
          {{else}}
            <tr>
              <td colspan="5" class="noResultTextReport">No Records Found</td>
            </tr>{{/hasElement}}
        </tbody>

      </table>
    </section>
    {{!-- <footer>{{{loadPage 'doctor_signature_footer.hbs' header}}}</footer> --}}

  </div>
</body>