version: "3"
services:
  core:
    container_name: ${CI_REGISTRY}/algaeh/algaeh-monorepo/algaeh-core
    env_file:
      - ./env
    build:
      context: ./algaeh-core
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
